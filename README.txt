how to run: 
on command line, run: ollama create codeDoctor -f modelfile
then run: ollama run codeDoctor

to run the requirements.txt file, run: pip install -r requirements.txt
then to run the file: python doctor.py

the ui will be available at http://127.0.0.1:7860